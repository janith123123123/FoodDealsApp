import React from "react";
import { Text, View } from "react-native";

const page = () => {
    return (
        <View>
            <Text>Profile</Text>
        </View>
    );
};

export default page;